"use client";

/**
 * useEditorDoc — ProseMirror/Convex sync lifecycle surface for a docId.
 * Skeleton implementation; replace with real sync and lifecycle wiring.
 */
export function useEditorDoc(docId: string): {
  docId: string;
  ready: boolean;
} {
  return { docId, ready: false };
}

