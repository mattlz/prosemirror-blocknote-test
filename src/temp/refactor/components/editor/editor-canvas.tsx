"use client";
import type { ReactElement } from "react";

/**
 * EditorCanvas — temporary skeleton component (core editor area).
 * Do not import into production UI until refactor step.
 */
export function EditorCanvas(): ReactElement | null {
  return null;
}

