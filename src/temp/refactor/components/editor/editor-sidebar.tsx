"use client";
import type { ReactElement } from "react";

/**
 * EditorSidebar — temporary skeleton component (side panels, comments, etc.).
 * Do not import into production UI until refactor step.
 */
export function EditorSidebar(): ReactElement | null {
  return null;
}

